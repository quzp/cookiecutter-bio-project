# Application version log

One line per version that was shared, received, frozen, finalized or submitted — the
same files that are copied into the section folders. Working saves (`wip`, `rev`) need
no entry; their file names are enough.

| Date (YYMMDD) | File | Status | What changed | Shared with / received from |
|---|---|---|---|---|
| | | | | |
