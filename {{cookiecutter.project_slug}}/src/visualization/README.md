# src/visualization/

Figure scripts. Input comes from `data/processed/` and `results/tables/`; output goes to `results/figures/`.

Shared style constants (palette, font sizes, dimensions) belong in a theme file under `src/utils/`, not duplicated in every script.
