# Manuscript version log

One line per version that was shared, received, finalized or submitted — the same files
that are copied up a level. Working saves (`wip`, `rev`) need no entry; their file names
are enough.

| Date (YYMMDD) | File | Status | What changed | Shared with / received from |
| --- | --- | --- | --- | --- |
| | | | | |
